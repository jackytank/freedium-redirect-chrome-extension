{
  "name": "freedium-redirect",
  "version": "0.3.0",
  "private": true,
  "description": "Chrome extension to redirect Medium articles to a Freedium mirror",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "typescript": "^6.0.3"
  }
}
